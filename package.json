{
  "name": "fraud-detection-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:angular": "cd frontend && npm start",
    "start:spring": "cd backend && mvnw spring-boot:run",
    "start:fastapi": "cd fraud-service && uv run uvicorn main:app",
    "start:shap": "cd fraud-service && uv run python shap_worker.py"
  }
}
